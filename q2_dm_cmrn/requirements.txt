# 问题2 DM-CMRN 工程依赖（本地 Windows / conda 环境 Lecture2env 已满足）
# torch 由 conda 提供：torch==2.6.0+cu124
numpy>=1.26,<2
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7            # 缺失规律曲线（可选）
transformers>=4.30         # 附件3 的 DistilBERT 文本重建（可选，见 --text-mode）

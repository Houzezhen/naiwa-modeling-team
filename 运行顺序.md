# 新代码运行顺序（本地 Windows / conda 环境 Lecture2env）

> 依据：仓库内 README、`speech_alignment_report.md`（问题1 复现顺序）、`run_multiview.sh`（问题2 SSL 分支）、问题2/问题3 报告中的复现命令，以及本机实跑验证。
> 环境：`D:\Anaconda\envs\Lecture2env\python.exe`（Python 3.10.9、torch 2.6.0+cu124、numpy 1.26.4、cv2 4.11.0、openpyxl 3.1.5、transformers 5.17.0、av 17.1.0、sentencepiece 0.2.0）。
> 依赖差异说明：仓库 `requirements-av.txt` 写的 `av==18.1.0` 与 `numpy>=2.0` 需要 Python≥3.11，本机 3.10 下改用 `av 17.1.0`（`pip install --no-deps av==17.1.0`）；`requirements-text.txt` 的 `transformers==4.29.2` 用 `5.17.0` 替代，均已实跑通过。首次下载预训练权重需要网络，本机已设 `HF_ENDPOINT=https://hf-mirror.com`。

## 0 目录与路径约定

```powershell
$repo = "E:\研究生数学建模\e题代码\naiwa-modeling-team"
$data = "E:\研究生数学建模\赛题\E题复杂场景下多模态情感识别的数学建模与算法设计\E题数据"
$py   = "D:\Anaconda\envs\Lecture2env\python.exe"
```

> **PowerShell 写法提示（易踩坑）**
> 1. 上面三行必须在**当前终端里先粘贴执行一次**；否则 `$py` 是空值，命令会变成以 `-u` 开头并报“表达式或语句中包含意外的标记 -u”。
> 2. 使用变量时前面要加调用运算符 `&`：`& $py -u train.py ...`（`&` 不能省略）。
> 3. 终端提示符已显示 `(Lecture2env)` 时，可直接把 `& $py` 换成 `python`，把 `"$data\..."` 换成绝对路径，这样完全不依赖变量。例如：
>
> ```powershell
> cd "E:\研究生数学建模\e题代码\naiwa-modeling-team\q2_baseline"
> python -u evaluate_attachment3.py --output attachment3_test_verify --device cpu
> ```
>
> （问题2 的脚本已内置本地默认数据路径；问题1/问题3 仍需显式传 `--data-root` / `--data` / `--attachment4`。）

问题1、问题2、问题3 三条线彼此独立，**唯一强依赖是：问题3 需要问题2 的 `q2_baseline/checkpoints/residual_fusion.pt`**。

## 1 问题2（`q2_baseline`）——推荐最先跑，已被完整复现

```powershell
cd "$repo\q2_baseline"

# 1)（可选）把 2.76 GB 未对齐特征重采样到 50 窗 → 生成 unaligned_resampled_50.pkl（约 3 分钟，峰值内存 ~3 GB）
& $py -u prepare_unaligned_50.py

# 2) 对齐版 baseline 训练（固定 baseline：5 轮，hidden 64，约 1–2 分钟）
& $py -u train.py --data "$data\附件2-数据集特征文件\aligned_50.pkl" --output q2_baseline_final --epochs 5 --device cuda:0

# 3) test 划分评估（--checkpoint 为必填，需同时给一个 multiview 权重）
& $py -u evaluate_test.py `
  --checkpoint checkpoints\residual_fusion.pt `
  --baseline-checkpoint q2_baseline_final\best.pt `
  --data "$data\附件2-数据集特征文件\aligned_50.pkl" `
  --output q2_test_eval --device cuda:0

# 4)（可选）长度感知重采样版 baseline：同样 5 轮
& $py -u train.py --data "$data\附件2-数据集特征文件\unaligned_resampled_50.pkl" --output q2_unaligned_baseline --epochs 5 --device cuda:0
& $py -u evaluate_test.py --checkpoint checkpoints\residual_fusion.pt `
  --baseline-checkpoint q2_unaligned_baseline\best.pt `
  --data "$data\附件2-数据集特征文件\unaligned_resampled_50.pkl" --output q2_test_eval_resampled --device cuda:0

# 5)（可选，重）标签自监督 + 残差融合：先 separate 再 hybrid，最后评估
& $py -u train_multiview.py --mode separate --hidden 128 --layers 2 --batch-size 64 `
  --encoder-epochs 10 --fusion-epochs 20 --freeze-epochs 3 `
  --encoder-learning-rate 3e-4 --fusion-learning-rate 2e-4 --amp --device cuda:0 --output q2_multiview_separate
& $py -u train_multiview.py --mode hybrid --hidden 128 --layers 2 --hybrid-batch-size 32 --hybrid-epochs 15 `
  --hybrid-learning-rate 1e-4 --consistency-weight 0.25 --logit-consistency-weight 0.5 `
  --init-dir q2_multiview_separate --amp --device cuda:0 --output q2_multiview_hybrid
& $py -u evaluate_test.py --checkpoint q2_multiview_separate\fusion_best.pt `
  --data "$data\附件2-数据集特征文件\aligned_50.pkl" --output q2_test_eval_multiview --device cuda:0

# 6) 附件3 专项推理（对齐版 + 未对齐版 × distilbert/zero 文本模式；CPU 约 3–5 分钟）
& $py -u evaluate_attachment3.py --checkpoint checkpoints\residual_fusion.pt --output attachment3_test --device cpu
```

辅助检查（随时可跑）：`inspect_devices.py` 查看 GPU/算子；`inspect_data.py <pkl>` 打印 splits 与字段形状。

## 2 问题1（`question1_alignment`）——与问题2/3 独立

正式版（声学约束对齐，输出 `output_speech_aware/`），顺序不可打乱：

```powershell
cd "$repo\E题复杂场景下多模态情感识别的数学建模与算法设计\question1_alignment"

# 1) 原视频特征准备（三模态基础特征；若 output\base_features.pkl 已存在可复用跳过）
& $py -u extract_av_features.py --data-root "$data" --work-dir output
# 2) 真实音频重建 + 有声活动候选 + 导出 50 窗波形
& $py -u prepare_speech_alignment.py --data-root "$data" --source-work-dir output --work-dir output_speech_aware
# 3) wav2vec2-CTC 词级强制对齐（需要样本的 audio_waveforms）
& $py -u force_align_transcripts.py --data-root "$data" --work-dir output_speech_aware
# 4) DistilBERT 最后 4 层均值 → 768 维文本特征（读取上一步的 text_alignment.jsonl）
& $py -u extract_text_features.py --work-dir output_speech_aware
# 5) 组装最终特征与核验表
& $py -u assemble_outputs.py --work-dir output_speech_aware --output-dir output_speech_aware
# 6) 典型样本帧导出
& $py -u export_typical_frames.py --data-root "$data" --output-dir output_speech_aware
# 7) 自动核验
& $py -u verify_outputs.py --output-dir output_speech_aware

# 可选：对齐质量指标与人工复核片段
& $py -u alignment_metrics.py --output-dir output_speech_aware --write
& $py -u render_alignment_examples.py --data-root "$data" --output-dir review_examples_speech_aware --alignment-output-dir output_speech_aware
```

旧版弱对齐（输出 `output/`，仅用于对照，勿与 `output_speech_aware/` 混用）：把上面 1)→4)→5)→6)→7) 的 work-dir 都换成 `output`，跳过第 2、3 步（文本对齐由 `extract_text_features.py` 内部按字符权重单调分配）。

## 3 问题3（`q3_explainability`）——依赖问题2 的 `residual_fusion.pt`

```powershell
cd "$repo\q3_explainability"

# 1) 后验解释：模态消融 + 50 窗遮挡 → 附件4 预测/贡献/证据（GPU 约 1–3 分钟）
#    注意：脚本会自动把附件4 结果写入 <output>\attachment4\，验证 JSON 写入 <output>\
& $py -u q2_posthoc_explain.py `
  --checkpoint "$repo\q2_baseline\checkpoints\residual_fusion.pt" `
  --data "$data\附件2-数据集特征文件\aligned_50.pkl" `
  --attachment4 "$data\附件4-可解释专项视频样本与特征文件\附件4-可解释专项视频样本与特征文件" `
  --output q3_q2_best_run_final --device cuda:0

# 2) 导出视觉证据关键帧（读取上一步生成的 q3_explanations.csv）
& $py -u export_video_keyframes.py `
  --explanations q3_q2_best_run_final\attachment4\q3_explanations.csv `
  --video-dir "$data\附件4-可解释专项视频样本与特征文件\附件4-可解释专项视频样本与特征文件\对齐版本\videos" `
  --output q3_q2_best_run_final\attachment4
```

## 4 总依赖图

```text
问题1  extract_av_features → prepare_speech_alignment → force_align_transcripts
        → extract_text_features → assemble_outputs → export_typical_frames → verify_outputs
问题2  prepare_unaligned_50(可选) → train.py → evaluate_test.py
        (可选) train_multiview separate → hybrid → evaluate_test.py
        evaluate_attachment3.py
问题3  q2_posthoc_explain.py → export_video_keyframes.py        ← 需要问题2 的 residual_fusion.pt
```

## 6 内存要求与已做的内存优化（均不改数值）

报错示例：`numpy.core._exceptions._ArrayMemoryError: Unable to allocate 497. MiB for an array with shape (3395, 50, 768)`。
原因：`train.py` 先 `pickle.load` 整个 `aligned_50.pkl`（约 0.95 GB 常驻），再为 `(3395,50,768)` 文本张量分配 1 份副本（497 MB）与 2–3 份临时数组，峰值约 2.5 GB。**这是内存不足，不是逻辑问题。**

已做三处语义等价的内存优化（输出数值逐位不变）：

| 文件 | 改动 |
|---|---|
| `train.py::prepare_split` | `values = np.clip((values - mean) / std, -10, 10)` → 原地 `values -= mean; values /= std; np.clip(..., out=values)` |
| `train.py::main` | 先 `data.pop("test", None)`（训练不用 test），训练划分特征张量建好后 `data.pop("train", None)` 再准备验证集 |
| `train_multiview.py` | `convert_split` 同样改为原地标准化；`load_view` 每转换完一个划分即释放该划分原始数组 |
| `prepare_unaligned_50.py` | 逐划分转换后立即释放该划分的 audio/vision（float64，占比最大） |

等价性验证：`train.py --epochs 5` 五轮指标与打补丁前**逐位一致**（epoch5 valid Acc 0.6112637362637363 / F1 0.5855523674970514 / MAE 0.622839741347419 / Pearson 0.6187776419980946）；`evaluate_test.py` 对 `residual_fusion.pt` 的 test 指标亦逐位一致（0.6286107290233838 / 0.5922199388722351 / 0.6738168206142756 / 0.634414501897337）。

建议运行前保证 **≥3 GB 可用内存**（关闭多余的 VS Code 窗口、Edge、Jupyter 内核等）；`prepare_unaligned_50.py` 处理 2.76 GB 输入时峰值最高，建议单独运行。

## 7 常见坑

1. `evaluate_test.py` 的 `--checkpoint` 是必填且可重复，想只评估 baseline 也必须再传一个 multiview 权重（未改逻辑，属作者设计）。
2. 附件3 的 DistilBERT 文本模式必须联网取权重；本机已设 `HF_ENDPOINT=https://hf-mirror.com`。
3. 重跑 `train.py` 会覆盖同名 `--output` 目录下的 `best.pt`；做对照实验请换 `--output`。
4. `output/` 与 `output_speech_aware/` 两套问题1 产物不得混用（作者在 README 与报告中明确）。
5. 内存：`unaligned_50.pkl` 2.76 GB、`aligned_50.pkl` 948 MB；`prepare_unaligned_50.py` 峰值约 3 GB。若 CUDA 报内存不足，先降 `--batch-size`/`--hybrid-batch-size`。
6. 现有关键结论均为**单次运行**，建议用 `problem2/run_seed_study.py` 的多种子协议复算后再写进论文。
